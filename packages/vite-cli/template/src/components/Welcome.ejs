<script setup lang="ts">
<% if (plugins.indexOf('vue-components') === -1) { -%>
import HelloWorld from '@/components/HelloWorld.vue'
<% } -%>
<% if (useRouter) { -%>
import { RouterLink, RouterView } from 'vue-router'
<% } -%>
<% if (usePinia) { -%>
import { storeToRefs } from 'pinia'
import { useCounterStore } from '@/store';
const counterStore = useCounterStore();
const { count } = storeToRefs(counterStore);
<% } -%>
</script>

<template>
  <header>
    <img
      alt="<%= components %> logo"
      class="logo"
      src="@/assets/<%= components %>.svg"
      width="125"
      height="125"
    />

    <div class="wrapper">
      <HelloWorld msg="You did it!" />

      <nav>
        <% if (useRouter) { -%>
          <RouterLink to="/">
          <% if (components === 'ant-design') { -%>
            <a-button type="primary">Home</a-button>
          <% } -%>
          <% if (components === 'element-plus') { -%>
            <el-button type="primary">Home</el-button>
          <% } -%>
          <% if (components === 'naive-ui') { -%>
            <n-button type="primary">Home</n-button>
          <% } -%>
          <% if (components === 'arco') { -%>
            <a-button type="primary">Home</a-button>
          <% } -%>
          <% if (components === 'tencent') { -%>
            <t-button theme="primary">Home</t-button>
          <% } -%>
          <% if (components === 'vuetify') { -%>
            <v-btn
            color="primary"
          >
            Home
          </v-btn>
          <% } -%>
          <% if (components === 'devui') { -%>
            <d-button variant="solid">Home</d-button>
          <% } -%>
          <% if (components === 'varlet') { -%>
            <var-button type="primary">Home</var-button>
          <% } -%>
        </RouterLink>
        <% } -%>
        <% if (useRouter) { -%>
          <RouterLink to="/about">
          <% if (components === 'ant-design') { -%>
            <a-button type="primary">About</a-button>
          <% } -%>
          <% if (components === 'element-plus') { -%>
            <el-button  type="primary">About</el-button>
          <% } -%>
          <% if (components === 'naive-ui') { -%>
            <n-button type="primary">About</n-button>
          <% } -%>
          <% if (components === 'arco') { -%>
            <a-button type="primary">About</a-button>
          <% } -%>
          <% if (components === 'tencent') { -%>
            <t-button theme="primary">About</t-button>
          <% } -%>
          <% if (components === 'varlet') { -%>
            <var-button type="primary">About</var-button>
          <% } -%>
          <% if (components === 'vuetify') { -%>
            <v-btn
            color="primary"
          >
          About
          </v-btn>
          <% } -%>
          <% if (components === 'devui') { -%>
            <d-button variant="solid">About</d-button>
          <% } -%>
          </RouterLink>
        <% } -%>
      </nav>
    </div>
  </header>
</template>
