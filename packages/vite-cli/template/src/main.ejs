import { createApp } from "vue";
import App from "./App.vue";
<% if (Router) { -%>
  import router from "./router";
<% } -%>
import "uno.css";
const app = createApp(App);
<% if (Router) { -%>
  app.use(router);
<% } -%>
app.mount("#app");
